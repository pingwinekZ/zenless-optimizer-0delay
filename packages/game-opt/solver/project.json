{
  "name": "game-opt-solver",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/game-opt/solver/src",
  "projectType": "library",
  "tags": [],
  "// targets": "to see all targets run: nx show project game-opt-solver --web",
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/coverage/libs/test"],
      "options": {
        "command": "bun test --jsdom",
        "cwd": "packages/game-opt/solver"
      }
    }
  }
}
